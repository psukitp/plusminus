import styled from 'styled-components'
import { SiderButtonComponent } from './SiderButton'

export const SiderButton = styled(SiderButtonComponent)`
  .sider-button {
    justify-content: start;
    align-items: center;
  }
`
